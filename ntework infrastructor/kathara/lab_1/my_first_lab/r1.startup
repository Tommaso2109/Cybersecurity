
# eth1 → PC1
ip addr add 192.168.1.1/24 dev eth1
ip link set eth1 up

# eth0 → R2
ip addr add 10.10.10.1/30 dev eth0
ip link set eth0 up

# Route verso LAN2 (S1)
ip route add 10.50.0.0/24 via 10.10.10.2

